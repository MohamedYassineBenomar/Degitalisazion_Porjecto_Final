# Pinned to the exact versions installed in the local venv so the
# Streamlit Cloud build reproduces the working setup. Bumping any of
# these requires re-testing on Cloud — Prophet in particular is
# sensitive to numpy/pandas major-version changes.
#
# build: v1.11-ml-dashboard
streamlit==1.57.0
prophet==1.3.0
pandas==3.0.2
plotly==6.7.0
numpy==2.4.4
scikit-learn==1.8.0
